# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Bu proje SDK 54'te (`package.json` → `expo: ~54.0.0`). Sürümü yükseltirken bu bağlantıyı da güncelle.
